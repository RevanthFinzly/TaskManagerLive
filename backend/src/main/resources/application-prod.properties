# Production Configuration

# H2 Database Configuration
spring.datasource.url=jdbc:h2:mem:crud_app
spring.datasource.username=sa
spring.datasource.password=
spring.datasource.driver-class-name=org.h2.Driver

# H2 Console (disabled in production for security)
spring.h2.console.enabled=false

# Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.H2Dialect

# Server Configuration
server.port=${PORT:8080}

# Logging Configuration
logging.level.org.springframework=ERROR
logging.level.com.example=INFO------- SEARCH
	<n>crud</n>
	<description>CRUD Application with Spring Boot and PostgreSQL</description>
=======
	<name>crud</name>
	<description>CRUD Application with Spring Boot and H2 Database</description>
+++++++ REPLACE